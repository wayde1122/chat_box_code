# AI Agent Hub 🤖

一个多 AI Agent 智能助手平台，采用 Next.js 14 构建。目前已上线「智能旅行助手」，可查询天气、推荐景点，为您的旅行提供智能规划。

## ✨ 功能特性

### 🏠 首页 - Agent Hub

- **Agent 展示平台** - 集中展示所有可用的 AI 助手
- **精美卡片设计** - 渐变动画、响应式布局
- **状态区分** - 可用 Agent 与敬请期待 Agent 分区展示

### 🛫 智能旅行助手

- **🌤️ 实时天气查询** - 查询全球任意城市的实时天气
- **📍 智能景点推荐** - 根据天气推荐最适合的旅游景点
- **🤖 AI 智能规划** - 基于 ReAct 模式的 Agent，一步步分析您的需求
- **💬 思考过程展示** - 可查看 AI 的完整思考过程和工具调用

### 🔮 即将推出

- **代码助手** - 智能编程助手，帮您解决开发难题
- **写作助手** - AI 写作伙伴，激发您的创作灵感
- **数据分析助手** - 智能分析数据，洞察业务趋势

---

## 🛠️ 技术栈

| 类别         | 技术                                    |
| ------------ | --------------------------------------- |
| **前端框架** | Next.js 14 + React 18 + TypeScript      |
| **UI 组件**  | Tailwind CSS + Shadcn UI + Lucide Icons |
| **AI 模型**  | DeepSeek-V3.2 (通过 ModelScope API)     |
| **天气 API** | wttr.in                                 |
| **搜索 API** | Tavily                                  |

---

## 🗺️ 路由结构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端路由                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   /                    ──→  首页 (Agent Hub)                │
│   │                         展示所有 Agent 列表              │
│   │                                                         │
│   └── /travel          ──→  智能旅行助手                     │
│                             聊天界面 + Agent 对话            │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                        后端 API                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   /api/chat            ──→  聊天接口 (POST)                  │
│                             支持 FAQ 匹配 / Agent 模式        │
│                                                             │
│   /api/faq             ──→  FAQ 数据接口 (GET)               │
│                             返回 FAQ 列表                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 页面说明

| 路由      | 页面         | 说明                                 |
| --------- | ------------ | ------------------------------------ |
| `/`       | 首页         | Agent Hub - 展示所有 AI 助手卡片     |
| `/travel` | 智能旅行助手 | 聊天对话界面，支持天气查询和景点推荐 |

### API 接口

| 接口        | 方法 | 说明          |
| ----------- | ---- | ------------- |
| `/api/chat` | POST | 聊天对话接口  |
| `/api/faq`  | GET  | 获取 FAQ 数据 |

---

## 🔌 API 详细说明

### POST /api/chat

聊天对话接口，支持多种模式。

**请求参数：**

```typescript
interface ChatRequestBody {
  question: string; // 用户问题
  model?: string; // 模型类型，默认 "travel-agent"
}
```

**模型类型：**

| model          | 说明                       |
| -------------- | -------------------------- |
| `travel-agent` | 智能旅行助手 Agent（默认） |
| `faq-matcher`  | FAQ 关键词匹配模式         |

**响应格式：**

```typescript
interface ChatResponse {
  model: string; // 使用的模型
  question: string; // 原始问题
  answer: string; // AI 回答
  steps?: AgentStep[]; // Agent 思考步骤（仅 Agent 模式）
  usedTools?: string[]; // 使用的工具列表（仅 Agent 模式）
}
```

**请求示例：**

```bash
curl -X POST http://localhost:3000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"question": "北京今天天气怎么样？"}'
```

### GET /api/faq

获取 FAQ 数据列表。

**响应格式：**

```typescript
interface FAQData {
  items: FAQItem[];
}

interface FAQItem {
  question: string;
  answer: string;
  keywords: string[];
}
```

---

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制环境变量模板并填入您的 API Key：

```bash
cp .env.example .env.local
```

编辑 `.env.local` 文件：

```env
# LLM 配置
LLM_API_KEY=your-api-key-here
LLM_BASE_URL=https://api-inference.modelscope.cn/v1
LLM_MODEL=deepseek-ai/DeepSeek-V3.2

# Tavily API 配置
TAVILY_API_KEY=your-tavily-api-key-here
```

### 3. 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 开始使用。

---

## 📖 使用示例

进入智能旅行助手后，尝试以下问题：

- "北京今天天气怎么样？"
- "上海明天天气适合去哪里玩？"
- "杭州后天天气如何，推荐一些景点"
- "深圳天气怎么样，有什么好玩的地方"

---

## 🏗️ 项目结构

```
src/
├── app/                          # Next.js App Router
│   ├── api/
│   │   ├── chat/route.ts         # 聊天 API - 支持 Agent/FAQ 模式
│   │   └── faq/route.ts          # FAQ 数据 API
│   ├── travel/
│   │   └── page.tsx              # 智能旅行助手页面
│   ├── layout.tsx                # 根布局
│   ├── page.tsx                  # 首页 (Agent Hub)
│   └── globals.css               # 全局样式
│
├── components/
│   ├── chat/                     # 聊天相关组件
│   │   ├── AgentSteps.tsx        # Agent 思考步骤展示
│   │   ├── ChatContainer.tsx     # 聊天容器
│   │   ├── ChatInput.tsx         # 输入框组件
│   │   ├── MessageItem.tsx       # 单条消息
│   │   ├── MessageList.tsx       # 消息列表
│   │   └── TypingIndicator.tsx   # 输入中指示器
│   ├── home/                     # 首页组件
│   │   ├── HeroSection.tsx       # Hero 区域
│   │   ├── AgentCard.tsx         # Agent 卡片
│   │   └── AgentGrid.tsx         # Agent 网格布局
│   ├── layout/                   # 布局组件
│   │   ├── ChatLayout.tsx
│   │   ├── Header.tsx
│   │   └── Sidebar.tsx
│   └── ui/                       # 基础 UI 组件
│       ├── button.tsx
│       ├── card.tsx
│       └── input.tsx
│
├── data/
│   ├── agents.ts                 # Agent 列表数据定义
│   └── faq.json                  # FAQ 数据
│
├── hooks/                        # React Hooks
│   ├── useChat.ts                # 聊天状态管理
│   ├── useFAQ.ts                 # FAQ 数据获取
│   └── useLocalStorage.ts        # 本地存储
│
├── services/                     # 服务层
│   ├── agentService.ts           # Agent 核心逻辑 (ReAct)
│   ├── attractionService.ts      # 景点搜索服务 (Tavily)
│   ├── chatService.ts            # 聊天服务
│   ├── faqService.ts             # FAQ 服务
│   ├── llmService.ts             # LLM 调用服务
│   ├── storageService.ts         # 本地存储服务
│   └── weatherService.ts         # 天气查询服务 (wttr.in)
│
└── types/                        # TypeScript 类型定义
    ├── agent.ts                  # Agent 相关类型
    ├── api.ts                    # API 请求/响应类型
    ├── chat.ts                   # 聊天消息类型
    └── faq.ts                    # FAQ 类型
```

---

## 🔧 架构说明

### 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                         用户界面                             │
│  ┌─────────────┐    ┌─────────────────────────────────────┐ │
│  │   首页      │    │         智能旅行助手                 │ │
│  │  Agent Hub  │───→│  ChatContainer + MessageList        │ │
│  └─────────────┘    └─────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        Hooks 层                              │
│           useChat.ts  /  useFAQ.ts  /  useLocalStorage.ts   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       Services 层                            │
│  chatService  │  agentService  │  weatherService  │  llmService │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       API 路由层                             │
│              /api/chat (POST)  │  /api/faq (GET)            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        外部服务                              │
│    DeepSeek LLM  │  wttr.in 天气  │  Tavily 搜索           │
└─────────────────────────────────────────────────────────────┘
```

### Agent 工作流程 (ReAct 模式)

本项目采用 **ReAct** (Reasoning + Acting) 模式：

1. **思考 (Thought)**: AI 分析用户请求，规划下一步行动
2. **行动 (Action)**: 调用工具获取信息（天气、景点等）
3. **观察 (Observation)**: 接收工具返回的结果
4. **循环**: 重复以上步骤直到收集足够信息
5. **完成 (Finish)**: 输出最终答案

### 可用工具

| 工具                            | 说明                       |
| ------------------------------- | -------------------------- |
| `get_weather(city, date?)`      | 查询指定城市的天气         |
| `get_attraction(city, weather)` | 根据城市和天气搜索推荐景点 |

---

## 🎨 UI 组件

### 首页组件

| 组件          | 说明                                               |
| ------------- | -------------------------------------------------- |
| `HeroSection` | Hero 区域 - 标题、副标题、品牌展示                 |
| `AgentCard`   | Agent 卡片 - 支持 featured 和 coming-soon 两种状态 |
| `AgentGrid`   | Agent 网格布局 - 响应式排列                        |

### 聊天组件

| 组件              | 说明                            |
| ----------------- | ------------------------------- |
| `ChatContainer`   | 聊天容器 - 整合消息列表和输入框 |
| `MessageList`     | 消息列表 - 展示对话历史         |
| `MessageItem`     | 单条消息 - 区分用户/AI 消息样式 |
| `ChatInput`       | 输入框 - 发送消息               |
| `AgentSteps`      | Agent 步骤 - 展示思考过程       |
| `TypingIndicator` | 输入中指示器                    |

---

## 📝 License

MIT
