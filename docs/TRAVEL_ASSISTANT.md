# 🛫 智能旅行助手 - 完整文档

> AI 驱动的智能旅行规划系统，基于多 Agent 协作架构，提供一站式旅行规划服务。

---

## 📖 目录

- [功能概览](#-功能概览)
- [系统架构](#-系统架构)
- [Agent 设计](#-agent-设计)
- [API 接口](#-api-接口)
- [组件说明](#-组件说明)
- [数据类型](#-数据类型)
- [配置说明](#-配置说明)
- [使用指南](#-使用指南)

---

## ✨ 功能概览

### 核心功能

| 功能 | 描述 | 状态 |
|------|------|------|
| 🗓️ **智能行程规划** | 根据目的地、日期、偏好自动生成完整行程 | ✅ 已完成 |
| 🗺️ **地图可视化** | 高德地图标注景点位置，直观展示行程 | ✅ 已完成 |
| 💰 **预算计算** | 自动计算门票、酒店、餐饮、交通费用 | ✅ 已完成 |
| ✏️ **行程编辑** | 支持添加、删除、调整景点 | ✅ 已完成 |
| 📤 **导出功能** | 导出为 PDF 或图片，方便保存分享 | ✅ 已完成 |
| 🖼️ **景点图片** | 异步加载景点图片（Unsplash API） | ✅ 已完成 |

### 技术亮点

- **多 Agent 协作** - 4 个专业 Agent 并行工作
- **MCP 协议集成** - 通过高德 MCP 获取真实数据
- **异步图片加载** - 不阻塞主流程，提升用户体验
- **响应式设计** - 适配桌面和移动端

---

## 🏗️ 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                           用户界面层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────┐ │
│  │  PlanForm    │  │ ItineraryList│  │ AMapContainer│  │BudgetPanel│ │
│  │  (表单输入)   │  │  (行程列表)   │  │   (地图)     │  │ (预算)    │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                           Hooks 层                                   │
│           useTravelPlan (状态管理)  │  useAMap (地图控制)            │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                           API 路由层                                 │
│  /api/travel/plan (生成计划)  │  /api/travel/images (获取图片)      │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         Agent 协作层                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐     │
│  │AttractionAgent  │  │  WeatherAgent   │  │   HotelAgent    │     │
│  │  (景点搜索)      │  │  (天气查询)      │  │  (酒店推荐)     │     │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘     │
│           │                    │                    │               │
│           └────────────────────┼────────────────────┘               │
│                                ▼                                    │
│                    ┌─────────────────────┐                          │
│                    │   PlannerAgent      │                          │
│                    │  (行程规划专家)       │                          │
│                    └─────────────────────┘                          │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          外部服务层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │  高德 MCP    │  │  Unsplash   │  │  LLM 服务    │              │
│  │ (POI/天气)   │  │  (图片API)   │  │ (智能规划)   │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
└─────────────────────────────────────────────────────────────────────┘
```

### 数据流

```
用户输入 → PlanForm → /api/travel/plan
                            │
                            ▼
              ┌─────────────────────────┐
              │     PlannerAgent        │
              │  (协调者 Agent)          │
              └─────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
      ┌──────────┐   ┌──────────┐   ┌──────────┐
      │景点Agent │   │天气Agent │   │酒店Agent │
      └──────────┘   └──────────┘   └──────────┘
            │               │               │
            └───────────────┼───────────────┘
                            ▼
              ┌─────────────────────────┐
              │      LLM 服务           │
              │  (生成完整行程)          │
              └─────────────────────────┘
                            │
                            ▼
                     返回 TravelPlan
                            │
                            ▼
              ┌─────────────────────────┐
              │   异步获取景点图片       │
              │   /api/travel/images    │
              └─────────────────────────┘
```

---

## 🤖 Agent 设计

### Agent 概览

| Agent | 职责 | 数据源 |
|-------|------|--------|
| `AttractionSearchAgent` | 根据用户偏好搜索景点 | 高德 MCP |
| `WeatherQueryAgent` | 查询目的地天气预报 | 高德 MCP |
| `HotelAgent` | 搜索符合条件的酒店 | 高德 MCP |
| `PlannerAgent` | 整合信息，生成完整计划 | LLM 服务 |

### 1. AttractionSearchAgent (景点搜索专家)

**职责：** 根据用户的旅行偏好搜索合适的景点

**输入：**
```typescript
interface AttractionSearchInput {
  city: string;           // 目的地城市
  preferences: string[];  // 用户偏好 ["history", "nature", ...]
  limit?: number;         // 返回数量限制
}
```

**偏好关键词映射：**
| 偏好 | 搜索关键词 |
|------|-----------|
| `history` | 历史、古迹、博物馆、故宫 |
| `nature` | 公园、自然风光、山、湖 |
| `food` | 美食街、小吃、特色餐厅 |
| `shopping` | 商场、购物中心、步行街 |
| `adventure` | 探险、户外、漂流、攀岩 |
| `relax` | 温泉、度假村、休闲 |
| `family` | 游乐园、动物园、海洋馆 |
| `romantic` | 浪漫、夜景、观景台 |

### 2. WeatherQueryAgent (天气查询专家)

**职责：** 查询旅行日期的天气预报

**输出：**
```typescript
interface WeatherInfo {
  date: string;
  description: string;  // "晴"、"多云"、"小雨"
  minTemp: number;
  maxTemp: number;
}
```

### 3. HotelAgent (酒店推荐专家)

**职责：** 根据预算和位置推荐酒店

**考虑因素：**
- 用户预算范围
- 靠近主要景点
- 酒店星级和评分

### 4. PlannerAgent (行程规划专家)

**职责：** 整合所有 Agent 的输出，生成完整行程

**工作流程：**
1. 并行调用 `AttractionAgent`、`WeatherAgent`、`HotelAgent`
2. 收集所有数据
3. 调用 LLM 生成智能行程安排
4. 计算预算明细
5. 返回完整的 `TravelPlan`

---

## 🔌 API 接口

### POST /api/travel/plan

生成旅行计划

**请求：**
```typescript
interface TravelRequest {
  destination: string;      // 目的地
  startDate: string;        // 出发日期 "2024-01-15"
  endDate: string;          // 返回日期 "2024-01-18"
  travelers: number;        // 出行人数
  preferences: string[];    // 偏好列表
  budget: "economy" | "moderate" | "luxury";  // 预算级别
}
```

**响应：**
```typescript
interface TravelPlanResponse {
  success: boolean;
  plan?: TravelPlan;
  duration?: number;  // 处理耗时 (ms)
  error?: string;
}
```

### POST /api/travel/images

批量获取景点图片

**请求：**
```typescript
{
  attractions: [{ id: string; name: string }];
  city: string;
}
```

**响应：**
```typescript
{
  images: { [attractionId: string]: string }  // id -> imageUrl
}
```

---

## 🧩 组件说明

### 页面组件

| 组件 | 路径 | 说明 |
|------|------|------|
| `TravelPlanner` | `components/travel/TravelPlanner.tsx` | 主页面容器 |

### 功能组件

| 组件 | 说明 |
|------|------|
| `PlanForm` | 旅行计划表单（目的地、日期、偏好等） |
| `ItineraryList` | 行程列表展示（按天分组） |
| `AMapContainer` | 高德地图容器（景点标注） |
| `BudgetPanel` | 预算明细面板（饼图可视化） |
| `ExportPanel` | 导出功能面板（PDF/图片） |

### Hooks

| Hook | 说明 |
|------|------|
| `useTravelPlan` | 旅行计划状态管理 |
| `useAMap` | 高德地图控制 |

---

## 📦 数据类型

### TravelPlan (完整行程)

```typescript
interface TravelPlan {
  id: string;
  destination: string;
  startDate: string;
  endDate: string;
  travelers: number;
  itinerary: ItineraryDay[];  // 每日行程
  hotels: Hotel[];            // 推荐酒店
  budget: BudgetBreakdown;    // 预算明细
  createdAt: number;
  updatedAt: number;
}
```

### ItineraryDay (单日行程)

```typescript
interface ItineraryDay {
  date: string;
  dayOfWeek: string;
  weather?: WeatherInfo;
  items: ItineraryItem[];
  dailyCost: number;
}
```

### ItineraryItem (行程项目)

```typescript
interface ItineraryItem {
  id: string;
  type: "attraction" | "restaurant" | "hotel" | "transport";
  startTime: string;
  endTime: string;
  cost: number;
  note?: string;
  attraction?: Attraction;
  restaurant?: Restaurant;
  hotel?: Hotel;
}
```

### BudgetBreakdown (预算明细)

```typescript
interface BudgetBreakdown {
  attractions: number;  // 门票
  hotels: number;       // 住宿
  meals: number;        // 餐饮
  transport: number;    // 交通
  others: number;       // 其他
  total: number;        // 总计
}
```

---

## ⚙️ 配置说明

### 环境变量

在 `.env.local` 中配置：

```bash
# ===== 高德地图配置 =====

# Web端(JS API) Key - 用于前端地图显示
NEXT_PUBLIC_AMAP_API_KEY=你的高德JS_API_Key

# 安全密钥 - 用于 JS API 验证
NEXT_PUBLIC_AMAP_SECURITY_KEY=你的安全密钥

# Web服务 API Key - 用于后端 MCP 调用
AMAP_API_KEY=你的Web服务Key

# ===== 图片 API 配置（任选其一）=====

# Unsplash - 免费 50次/小时
UNSPLASH_ACCESS_KEY=你的Unsplash_Key

# Pexels - 免费 200次/小时（推荐）
PEXELS_API_KEY=你的Pexels_Key

# ===== LLM 配置 =====
LLM_API_KEY=你的LLM_API_Key
LLM_BASE_URL=https://api-inference.modelscope.cn/v1
LLM_MODEL=deepseek-ai/DeepSeek-V3.2
```

### 获取 API Key

| 服务 | 获取地址 | 说明 |
|------|---------|------|
| 高德地图 | [console.amap.com](https://console.amap.com/) | 需创建 Web端 和 Web服务 两个 Key |
| Unsplash | [unsplash.com/oauth](https://unsplash.com/oauth/applications) | 免费 50次/小时 |
| Pexels | [pexels.com/api](https://www.pexels.com/api/new/) | 免费 200次/小时 |

---

## 📱 使用指南

### 1. 填写旅行信息

- 输入目的地城市
- 选择出发和返回日期
- 设置出行人数
- 选择旅行偏好（可多选）
- 选择预算级别

### 2. 生成行程

点击"生成智能行程"按钮，系统将：
1. 搜索符合偏好的景点
2. 查询天气预报
3. 推荐合适酒店
4. AI 智能规划每日行程
5. 计算详细预算

### 3. 查看和编辑

- **行程列表** - 查看每日安排，可删除不感兴趣的景点
- **地图** - 在地图上查看景点分布
- **预算** - 查看费用明细和占比

### 4. 导出分享

- 导出为**图片** - 保存到相册
- 导出为 **PDF** - 打印或发送

---

## 🔗 相关链接

- [返回主文档](../README.md)
- [高德 MCP 文档](https://lbs.amap.com/)
- [Unsplash API 文档](https://unsplash.com/documentation)

---

## 📝 更新日志

### v1.0.0 (2024-12)

- ✅ 完成多 Agent 协作架构
- ✅ 集成高德 MCP 服务
- ✅ 实现地图可视化
- ✅ 添加预算计算和饼图展示
- ✅ 支持 PDF/图片导出
- ✅ 异步加载景点图片

